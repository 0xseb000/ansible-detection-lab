[defaults]
inventory=inventory/hosts.yml
# galaxy first: `ansible-galaxy install` writes to the first position
roles_path=roles/galaxy:roles
collections_path=collections

[privilege_escalation]
become=True
become_method=sudo
become_ask_pass=False
