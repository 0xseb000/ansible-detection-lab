---
w_prometheus_web_listen_address: "0.0.0.0:9090"

w_prometheus_scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - "localhost:9090"

# Own Detection-Rules
w_prometheus_alert_rules: []
